# Benchmarks

Use this directory for model and provider comparison inputs, outputs, and notes.

Every benchmark should identify:

- fixture set
- contract version
- evaluated providers or models
- scoring rubric
