# Pipelines

Home for agent orchestration and multi-step experiment flows.

Examples:

- extraction -> validation -> persistence flows
- retrieval -> ranking -> response planning experiments
- agent handoff pipelines under fixture control
