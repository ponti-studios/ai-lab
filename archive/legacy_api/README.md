# Legacy Product API Snapshot

This directory preserves the pre-repurpose FastAPI product backend so prompt
history, router logic, and earlier storage patterns remain inspectable.

It is intentionally archived:

- do not add new product features here
- do not treat this as the active Hominem API
- migrate reusable ideas into the new lab surfaces when they become durable
